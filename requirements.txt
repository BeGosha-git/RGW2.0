# Requirements compatible with Python 3.8, 3.11, and 3.13.
# No strict upper bounds — pip resolves the highest wheel that supports the
# running Python automatically (e.g. numpy 1.24.x on 3.8, 2.x on 3.13).

# Web framework (flask 3.0.x is the last release that supports Python 3.8;
# pip will install 3.1.x on Python 3.9+)
flask>=2.3.0
werkzeug>=2.3.0
jinja2>=3.1.0
markupsafe>=2.1.0
itsdangerous>=2.1.2
click>=8.1.0

# HTTP / networking
requests>=2.28.0
aiohttp>=3.9.0
websockets>=11.0

# WebRTC
aiortc>=1.9.0
# av 11+ requires Python 3.9+; pip installs the highest compatible version
av>=9.0.0

# Vision / media
# opencv-python 4.x supports Python 3.8-3.13
opencv-python>=4.5.0
# Pillow 11 requires Python 3.9+; pip installs 10.x on 3.8 automatically
Pillow>=9.0.0
# numpy 2.x requires Python 3.9+; pip installs 1.24.x on 3.8 automatically
numpy>=1.20.0

# Hardware (optional — failures are tolerated)
pyrealsense2>=2.50.0
cyclonedds==0.10.2

# Desktop / UI
mss>=6.1.0
pynput>=1.7.0
